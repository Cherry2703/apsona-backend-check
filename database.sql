-- CREATE TABLE user(
-- user_id int PRIMARY key,
-- user_name varchar(50) NOT NULL,
-- user_password varchar(300) NOT NULL);

-- insert into user(user_id,user_name,user_password)
-- VALUES(1,'Ram Charan Teja','Ram@123'),(2,'Nikhil','Nikhil@123');

-- create TABLE notes(
-- note_id int PRIMARY key,
-- note_title varchar(50) NOT NULL,
-- note_content varchar(300) not null);

-- insert into notes(note_id,note_title,note_content)
-- VALUES(1001,'React','React is a javascript library'),
-- (1002,'Angular','Angular is a javascript framework');

-- select * from notes;

-- CREATE table archive(note_id int PRIMARY KEY,note_title varchar(50) not null,note_content varchar(500) not null);
-- insert into archive(note_id,note_title,note_content)
-- VALUES(1001,'React','React is a javascript library'),
-- (1002,'Angular','Angular is a javascript framework');

-- select * from archive;

-- CREATE table trash(note_id int PRIMARY KEY,note_title varchar(50) not null,note_content varchar(500) not null);
-- insert into trash(note_id,note_title,note_content)
-- VALUES(1100,'HTML','HTML is used to create structure'),
-- (1101,'Angulavdfv','Angular is a jadvfdvvascript framework');

-- select * from trash;


